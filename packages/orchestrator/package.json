{
  "name": "@ureru-buzz-ai/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ureru-buzz-ai/core": "workspace:*",
    "@ureru-buzz-ai/agents": "workspace:*"
  }
}
