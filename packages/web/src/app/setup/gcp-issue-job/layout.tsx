import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "GitHub連携の準備（かんたん） | 売れるBUZZ AI",
  description: "Google Cloud と GitHub をつなぐための手順と記入欄。非エンジニア向け。",
};

export default function SetupGcpIssueJobLayout({ children }: { children: React.ReactNode }) {
  return children;
}
